# prayagupd.github.io

Personal homepage — a single, dependency-free static page.

- `index.html` — the site (semantic HTML, JSON-LD `Person` schema, OG/meta tags, light/dark via `prefers-color-scheme`)
- `llms.txt` — machine-readable summary for LLMs and crawlers
